# iTeam Agent Client

> Electron桌面应用 - 集成Claude Code CLI的智能Agent客户端

## 功能特性

### ✨ 核心功能

- 🖥️ **跨平台桌面应用** - 基于Electron，支持Windows、macOS和Linux
- 🤖 **Claude Code集成** - 自动调用Claude Code CLI执行编程任务
- 🔗 **实时服务器连接** - 通过WebSocket连接iTeam服务器
- 📋 **任务队列管理** - 自动接收并按顺序执行任务
- 👤 **角色和技能配置** - 灵活设置Agent的角色和技能标签
- 📊 **实时状态监控** - 实时显示任务执行状态和日志
- ❤️ **心跳机制** - 自动保持与服务器的连接

### 🎨 界面特性

- 现代化的暗色主题UI设计
- 实时日志终端
- 任务列表可视化
- 连接状态指示器
- 配置保存到本地存储

## 安装

### 前提条件

1. **Node.js** >= 18.0.0
2. **Claude Code CLI** - 已安装并配置好API Key

安装Claude Code CLI:
```bash
npm install -g @anthropic/claude-code
```

配置API Key:
```bash
export ANTHROPIC_API_KEY="your-api-key-here"
```

### 安装依赖

```bash
cd agent-client
npm install
```

## 使用方法

### 开发模式运行

```bash
npm run dev
```

这将以开发模式启动应用，并自动打开开发者工具。

### 生产模式运行

```bash
npm start
```

### 打包应用

```bash
# 打包所有平台
npm run build

# 仅打包Windows
npm run build:win

# 仅打包macOS
npm run build:mac

# 仅打包Linux
npm run build:linux
```

## 配置指南

### 1. 服务器连接设置

在应用界面的左侧面板中配置：

- **服务器地址**: iTeam服务器的URL（例如：`http://localhost:3000`）
- **API Key**: 设备认证密钥（默认：`iteam-device-key`）

### 2. Agent配置

设置您的Agent信息：

- **设备名称**: 给您的设备起一个易识别的名称
- **角色**: 选择Agent的角色
  - Frontend Developer
  - Backend Developer
  - Full Stack Developer
  - DevOps Engineer
  - QA Engineer
  - UI/UX Designer
- **技能标签**: 用逗号分隔的技能列表（例如：`react,nodejs,docker`）

### 3. 连接到服务器

1. 配置好服务器地址和API Key
2. 配置好Agent信息
3. 点击"保存配置"按钮
4. 点击"连接服务器"按钮

连接成功后，状态指示器会变为绿色，显示"已连接"。

## 工作流程

### 任务自动化流程

```
iTeam服务器 → WebSocket → Agent客户端 → 任务队列 → Claude Code CLI → 执行结果 → 服务器
```

1. **任务接收**: Agent连接到iTeam服务器后，会自动接收分配的任务
2. **队列管理**: 任务被添加到本地队列中
3. **自动执行**: Agent按顺序执行队列中的任务
4. **调用Claude Code**: 根据任务类型和描述，自动调用Claude Code CLI
5. **状态同步**: 实时向服务器报告任务执行状态
6. **结果反馈**: 任务完成后将结果发送回服务器

### 支持的任务类型

- **code_generation**: 代码生成任务
- **code_review**: 代码审查任务
- **bug_fix**: Bug修复任务
- **test_generation**: 测试生成任务
- **refactor**: 代码重构任务
- **custom**: 自定义任务（直接传递prompt）

## 项目结构

```
agent-client/
├── src/
│   ├── main/              # 主进程
│   │   └── main.js        # Electron主进程入口
│   ├── renderer/          # 渲染进程
│   │   ├── index.html     # 界面HTML
│   │   ├── styles.css     # 样式文件
│   │   └── renderer.js    # 渲染进程逻辑
│   ├── preload/           # 预加载脚本
│   │   └── preload.js     # IPC通信桥梁
│   └── services/          # 服务层
│       ├── socket-service.js   # WebSocket连接服务
│       └── claude-service.js   # Claude Code调用服务
├── package.json
└── README.md
```

## 技术栈

- **Electron** - 跨平台桌面应用框架
- **Socket.io-client** - WebSocket实时通信
- **Node.js** - 后端运行时
- **Claude Code CLI** - AI编程助手

## 开发说明

### 安全性

应用使用Electron的最佳安全实践：

- ✅ `contextIsolation`: true
- ✅ `nodeIntegration`: false
- ✅ `enableRemoteModule`: false
- ✅ 使用preload脚本作为IPC桥梁
- ✅ CSP (Content Security Policy) 配置

### 环境变量

```bash
# Claude API Key（必需）
ANTHROPIC_API_KEY=your-api-key-here

# iTeam服务器地址（可选，可在界面配置）
ITEAM_SERVER_URL=http://localhost:3000

# iTeam API Key（可选，可在界面配置）
ITEAM_API_KEY=iteam-device-key
```

## 故障排查

### Claude Code未找到

如果看到"Claude Code CLI not found"错误：

1. 确认已安装Claude Code: `npm install -g @anthropic/claude-code`
2. 确认命令可用: `claude --version`
3. 检查PATH环境变量

### 连接服务器失败

1. 检查iTeam服务器是否运行
2. 确认服务器地址正确
3. 检查API Key是否正确
4. 查看防火墙设置

### 任务执行失败

1. 检查Claude API Key是否配置正确
2. 查看执行日志中的错误信息
3. 确认任务的工作目录存在
4. 检查任务描述是否清晰

## 许可证

MIT License

## 相关链接

- [iTeam主项目](../README.md)
- [iTeam服务器](../server/README.md)
- [Claude Code文档](https://docs.anthropic.com/claude-code)

---

**开发者**: iTeam Team
**版本**: 0.1.0
**最后更新**: 2026-01-17
